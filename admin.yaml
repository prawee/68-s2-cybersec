services:
  s2admin:
    image: dpage/pgadmin4:latest
    container_name: 68-s2-admin
    ports:
      - ${ADMIN_PORT}:80
    environment:
      PGADMIN_DEFAULT_EMAIL: ${ADMIN_DEFULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${ADMIN_DEFULT_PASS}